// Wait until the DOM is fully loaded
document.addEventListener("DOMContentLoaded", function() {
    // Select the div
    let myDiv = document.getElementById("myDiv");
    
    // Change the text inside the div
    myDiv.innerText = "Text changed by JavaScript!";
});
